#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define FORInput(a,b,c) for(int (a) = b; (a)<(c);(a)++)

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int n;
    cin>>n;
    
    int arr[n];
    
    FORInput(i,0,n) cin>>arr[i];
    
    
    
    return 0;
}